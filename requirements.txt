# Molecular Docking in the AI Era -- companion repository.
#
#   python3.12 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# The book's numbers were produced with pip on Ubuntu, Python 3.12.3.
# This file, not environment/environment.yml, is the authoritative environment.
#
# ON WINDOWS THIS FILE DOES NOT INSTALL AS IT STANDS. The vina line below has
# no Windows wheel, pip falls back to the sdist, the build fails during
# resolution, and NOTHING is installed -- not the failing package, everything.
# environment/README.md gives the two-command version that works.
#
# Six of these can change a published value -- pinned exactly:
#   rdkit, vina, spyrmsd, meeko, numpy, and Open Babel.
vina==1.2.7            # NOT 1.2.5 -- superseded February 2025.
#                        NOTE: this is the PYTHON BINDING. It gives you no
#                        `vina` command, and every script here calls Vina
#                        through its command line. You need the binary too:
#                        see environment/README.md. Verified on Ubuntu 24.04 --
#                        pip succeeds and 69 tests then error on "Vina not found".
rdkit==2026.3.5        # conformer counts in ch08 are version-sensitive
spyrmsd==0.9.0         # symmetry-corrected RMSD; never run it with --minimize
meeko==0.8.0           # PDBQT preparation
gemmi==0.7.5           # meeko needs it; pinned because meeko's output depends on it
numpy==2.4.4

# Plots only -- a minimum bound is enough, these cannot move a published number.
matplotlib>=3.10.8
scipy>=1.17.1

# The test suite is this repository's main evidence, and the root README tells
# you to run it. It was missing from this file, so a reader who followed the
# documented install got "No module named pytest" at the first thing they were
# asked to do. A minimum bound: pytest cannot move a published number.
pytest>=8.0

# Open Babel 3.2.1 -- the sixth pin that can move a published value, since
# ch04's format round-trips are literally its output. Verify the version you
# got rather than assuming your distribution's.
#
#   sudo apt install openbabel        # Ubuntu -- check `obabel -V` says 3.2.1
#
# On Windows there is a pip route, and it is what this repository's own
# Windows environment uses:
#
#   pip install openbabel-wheel==3.1.1.23
#
# Note the version it actually gives you: **Open Babel 3.1.0, not 3.2.1** --
# openbabel-wheel ships no 3.2.1 build for Windows. Recorded here rather than
# glossed, because Open Babel is on the list of things that can change a
# number. It did not change any of ch04's conclusions, and
# ch04_formats/outputs/expected/results.md says so and says which version
# produced them.
